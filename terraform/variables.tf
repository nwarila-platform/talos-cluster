# Module inputs.
#
# Every variable MUST set `nullable = false` for required inputs and include
# a `validation` block when the input has constraints beyond its type. See
# the iso-manager reference implementation for the canonical pattern.
