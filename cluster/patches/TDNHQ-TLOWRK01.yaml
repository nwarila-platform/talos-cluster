# =============================================================================
# TDNHQ-TLOWRK01 — Worker Node 1
# IP: 10.69.112.68 | Disk: nvme0n1 (250GB)
# =============================================================================
machine:
  install:
    disk: /dev/nvme0n1
  network:
    interfaces:
      - deviceSelector:
          physical: true
        addresses:
          - 10.69.112.68/24
        routes:
          - network: 0.0.0.0/0
            gateway: 10.69.112.1
