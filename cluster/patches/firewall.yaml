# =============================================================================
# Talos host ingress firewall — TEMPORARILY DISABLED during recovery.
#
# Original config caused cluster lockout (operator workstation at 10.69.12.0/24
# was not in the 10.69.112.0/24 allow rules). After recovery, this file will
# be rewritten with operator subnet included in all allow rules.
#
# Talos host firewall behavior observed 2026-05-27:
# - NetworkRuleConfig with ingress[].subnet acts as a PER-PORT ALLOWLIST,
#   not just an "allow this in addition to default" rule. Even when
#   NetworkDefaultActionConfig is `accept`, ports covered by a
#   NetworkRuleConfig become restricted to the listed subnets.
# - To fully disable the firewall, ALL NetworkRuleConfig documents must
#   be removed AND NetworkDefaultActionConfig must be accept (or absent).
#
# Recovery state: this file intentionally contains NO firewall documents.
# generate.sh still appends it via append_firewall() but it produces zero
# active rules. Talos nftables ingress chain will be in default-accept
# state (no chains at all, equivalent to pre-Step-4 baseline).
# =============================================================================
