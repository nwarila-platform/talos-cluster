# =============================================================================
# cp3 — Control Plane Node 3
# Asset name: TDNHQ-TLOMGT03 | IP: 10.69.112.65 | Disk: nvme0n1
#
# Interface split: see cp1.yaml for rationale.
# =============================================================================
machine:
  install:
    disk: /dev/nvme0n1
  network:
    interfaces:
      - deviceSelector:
          physical: true
        dhcp: false
        vip:
          ip: 10.69.112.62
      - deviceSelector:
          physical: true
        addresses:
          - 10.69.112.65/24
        routes:
          - network: 0.0.0.0/0
            gateway: 10.69.112.1
