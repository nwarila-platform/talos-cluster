# =============================================================================
# Metrics Server — Helm values
# Chart: metrics-server/metrics-server (version 3.13.0 at adoption)
#
# Authoritative values are in
# clusters/talos-cluster/apps/metrics-server/helmrelease.yaml — Flux is
# the canonical reconciler. This file is the human-readable copy retained
# for the README's install command and for the ADR cross-reference.
#
# As of Sub-cycle B (2026-05-26): metrics-server uses its chart defaults
# and validates kubelet's serving cert against the cluster CA (signed via
# rotate-server-certificates + postfinance/kubelet-csr-approver, see
# ADR-0005). The previous `args: [--kubelet-insecure-tls]` workaround
# is no longer needed and is removed.
# =============================================================================
# (intentionally empty: chart defaults are correct for this cluster)
