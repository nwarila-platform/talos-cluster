# =============================================================================
# Kyverno - Helm values
# Chart: kyverno/kyverno (version 3.8.1 at adoption)
#
# Authoritative values are in
# clusters/talos-cluster/apps/kyverno/helmrelease.yaml - Flux is the canonical
# reconciler. This file is the human-readable copy retained for review and
# ADR cross-reference.
# =============================================================================
admissionController:
  replicas: 3
backgroundController:
  replicas: 2
cleanupController:
  replicas: 2
reportsController:
  replicas: 2
networkPolicy:
  enabled: true
