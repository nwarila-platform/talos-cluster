# Child reconciliation for the hwg-only Traefik ingress proxy. Keeping this
# inventory under one prune boundary makes rollback remove the proxy, its class,
# its scoped RBAC, and both sides of its network-policy contract together.
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: traefik-hwg
  namespace: flux-system
spec:
  interval: 10m
  path: ./clusters/talos-cluster/apps/traefik-hwg
  prune: true
  wait: true
  timeout: 10m
  sourceRef:
    kind: GitRepository
    name: flux-system
  healthChecks:
    - apiVersion: apps/v1
      kind: Deployment
      name: traefik-hwg
      namespace: traefik-hwg
