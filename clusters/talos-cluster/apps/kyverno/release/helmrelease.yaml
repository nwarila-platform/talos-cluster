# HelmRelease - Kyverno policy engine.
#
# This installs the policy/admission engine only. Image verification policies
# are intentionally a follow-up change after the CRDs and controllers are live.
# See docs/decision-records/repo/0010-adopt-kyverno-policy-engine.md.
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: kyverno
  namespace: kyverno
spec:
  interval: 10m
  releaseName: kyverno
  chart:
    spec:
      chart: kyverno
      version: 3.8.2
      sourceRef:
        kind: HelmRepository
        name: kyverno
      interval: 1h
  install:
    crds: Create
  upgrade:
    crds: CreateReplace
    remediation:
      remediateLastFailure: false
    cleanupOnFail: true
  values:
    # Kyverno publishes cosign-verifiable release images to GHCR. The
    # reg.kyverno.io mirrors do not expose discoverable signatures for v1.18.2.
    global:
      image:
        registry: ghcr.io
    admissionController:
      replicas: 3
      networkPolicy:
        enabled: true
    backgroundController:
      replicas: 2
      networkPolicy:
        enabled: true
    cleanupController:
      replicas: 2
      networkPolicy:
        enabled: true
    reportsController:
      replicas: 2
      networkPolicy:
        enabled: true
  test:
    enable: false
