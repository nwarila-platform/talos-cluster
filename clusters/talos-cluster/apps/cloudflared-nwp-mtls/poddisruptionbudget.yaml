apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: cloudflared-nwp-mtls
  namespace: cloudflared-nwp-mtls
spec:
  maxUnavailable: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: cloudflared
      app.kubernetes.io/instance: nwp-mtls
