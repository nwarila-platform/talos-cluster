apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Relocated from nwarila-platform/deploy-vault@bf7f0e0:
# kubernetes/overlays/talos-cluster. Keep this overlay semantically identical
# to the source pin; follow-up policy changes happen in later PRs.
resources:
  - base
