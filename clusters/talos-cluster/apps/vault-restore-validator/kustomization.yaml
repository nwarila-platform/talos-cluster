apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Boundary skeleton only. Do not add this path to the parent apps
# kustomization until ADR-0020 is accepted and the first implementation PR is
# reviewed for production impact.
namespace: dr-validate

resources:
  - namespace.yaml
  - serviceaccounts.yaml
  - rbac.yaml
  - networkpolicy-default-deny.yaml
