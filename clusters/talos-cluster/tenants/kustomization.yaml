# Kustomize index for onboarded tenant security envelopes.
#
# Tenant directories are added here by .github/workflows/onboard-tenant.yaml.
# The repository starts with no tenants, so the resource list is intentionally
# empty until the first onboarding PR is merged.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources: []
