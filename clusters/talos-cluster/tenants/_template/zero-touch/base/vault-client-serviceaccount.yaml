apiVersion: v1
kind: ServiceAccount
metadata:
  name: vault-client
  namespace: tenant-placeholder
  labels:
    nwarila.io/platform-owned: "true"
    nwarila.io/vault-client: "true"
imagePullSecrets:
  # Tenants using only public images may omit this Secret; the dangling
  # reference is harmless until a private pull needs credentials.
  - name: ghcr-pull
