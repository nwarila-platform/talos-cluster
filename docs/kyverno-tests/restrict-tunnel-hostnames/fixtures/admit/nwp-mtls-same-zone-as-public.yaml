apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: nwp-mtls-same-zone-as-public
  namespace: nwp-1306985678
spec:
  ingressClassName: cf-tunnel-nwp-mtls
  rules:
    - host: app.nickwarila.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: app
                port:
                  number: 8080
