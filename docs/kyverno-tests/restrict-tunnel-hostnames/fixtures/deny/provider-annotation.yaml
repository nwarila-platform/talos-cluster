apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: provider-annotation
  namespace: hwg-1268831311
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: web
spec:
  ingressClassName: cf-tunnel-hwg
  rules:
    - host: annotated.theherowarsguys.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: app
                port:
                  number: 8080
