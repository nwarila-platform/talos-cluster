apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: resource-backend
  namespace: hwg-1268831311
spec:
  ingressClassName: cf-tunnel-hwg
  rules:
    - host: resource.theherowarsguys.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              resource:
                apiGroup: example.com
                kind: StorageBucket
                name: app
