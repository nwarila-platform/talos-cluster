apiVersion: cli.kyverno.io/v1alpha1
kind: Value
metadata:
  name: restrict-tunnel-binding-values
namespaces:
  - apiVersion: v1
    kind: Namespace
    metadata:
      name: hwg-1268831311
      labels:
        nwarila.io/tenant: "true"
        nwarila.io/org: the-hero-wars-guys
  - apiVersion: v1
    kind: Namespace
    metadata:
      name: nwp-1306985678
      labels:
        nwarila.io/tenant: "true"
        nwarila.io/org: nwarila-platform
  - apiVersion: v1
    kind: Namespace
    metadata:
      name: deploy-vault
      labels:
        nwarila.io/tenant: "true"
  - apiVersion: v1
    kind: Namespace
    metadata:
      name: unregistered-tenant
      labels:
        nwarila.io/tenant: "true"
        nwarila.io/org: unregistered-org
  - apiVersion: v1
    kind: Namespace
    metadata:
      name: flux-system
  - apiVersion: v1
    kind: Namespace
    metadata:
      name: cloudflared-hwg
  # Kyverno CLI assigns a synthetic default namespace while evaluating a
  # cluster-scoped fixture. Kubernetes ignores namespaceSelector for
  # cluster-scoped resources, so label the CLI's synthetic Namespace to keep
  # the IngressClass validation on the offline evaluation path.
  - apiVersion: v1
    kind: Namespace
    metadata:
      name: default
      labels:
        nwarila.io/tenant: "true"
