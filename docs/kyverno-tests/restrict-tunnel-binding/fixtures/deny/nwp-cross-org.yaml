apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: nwp-cross-org
  namespace: nwp-1306985678
spec:
  ingressClassName: cf-tunnel-hwg
  defaultBackend:
    service:
      name: probe
      port:
        number: 80
