apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: legacy-annotation-only
  namespace: hwg-1268831311
  annotations:
    kubernetes.io/ingress.class: cf-tunnel-hwg
spec:
  defaultBackend:
    service:
      name: probe
      port:
        number: 80
