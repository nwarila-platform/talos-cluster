apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: legacy-cross-org
  namespace: hwg-1268831311
  annotations:
    kubernetes.io/ingress.class: cf-tunnel-nwp
spec:
  ingressClassName: cf-tunnel-hwg
  defaultBackend:
    service:
      name: probe
      port:
        number: 80
