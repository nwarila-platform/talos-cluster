name: Terraform Static CI

on:
  pull_request:
    paths:
      - "terraform/**"
      - ".github/workflows/**"
      - "iam/**"

permissions:
  contents: read

concurrency:
  group: terraform-static-ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  actionlint:
    name: actionlint
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          persist-credentials: false
      - name: Run actionlint
        run: |
          curl -sSLo actionlint.tar.gz https://github.com/rhysd/actionlint/releases/download/v1.7.1/actionlint_1.7.1_linux_amd64.tar.gz
          tar -xzf actionlint.tar.gz actionlint
          ./actionlint -color

  terraform:
    name: terraform fmt + validate
    runs-on: ubuntu-latest
    permissions:
      contents: read
    strategy:
      fail-fast: false
      matrix:
        stack: [bootstrap, platform]
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          persist-credentials: false
      - uses: hashicorp/setup-terraform@5e8dbf3c6d9deaf4193ca7a8fb23f2ac83bb6c85 # v4.0.0
        with:
          terraform_version: 1.9.5
      - name: terraform fmt -check -recursive
        working-directory: terraform/${{ matrix.stack }}
        run: terraform fmt -check -recursive
      - name: terraform init -backend=false
        working-directory: terraform/${{ matrix.stack }}
        run: terraform init -backend=false
      - name: terraform validate
        working-directory: terraform/${{ matrix.stack }}
        run: terraform validate
