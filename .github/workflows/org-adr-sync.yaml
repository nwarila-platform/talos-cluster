name: Org ADR Sync

# RESTORED: NWarila/drift-gate is allowlisted via P0.1.
# PR-time enforcement is active again (TD-0004 closed) and posts
# the `org-adr / verify` check on every pull request to main.
on:
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  checks: write

concurrency:
  group: org-adr-sync-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  baseline:
    name: org-adr / verify
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    steps:
      - name: Drift gate (org ADR baseline)
        # renovate: datasource=git-refs depName=NWarila/drift-gate
        uses: NWarila/drift-gate@dac07c2b2a30a084b3577d4712549291ae99879c
        with:
          source-repo: nwarila-platform/.github
          # renovate: datasource=git-refs depName=nwarila-platform/.github
          source-ref: 0d2a0752d4c618907d735af4a7c12a9214c8517e
          manifest: org-adr-manifest.json
          check-name: org-adr / verify
